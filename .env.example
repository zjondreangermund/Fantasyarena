# Database Configuration
# Railway will automatically set this when you add a PostgreSQL service
DATABASE_URL=postgresql://username:password@host:port/database

# Environment
NODE_ENV=production

# Server Port
# Railway will automatically set this - you typically don't need to set it manually
PORT=5000

# Optional: Replit Integration (only needed if running on Replit)
# REPL_ID=your-repl-id
