# Environment Variables for Fantasy Arena
# 
# 📖 For detailed guidance, see: ENVIRONMENT_VARIABLES.md
# 
# ⚡ If using Render Blueprint or Railway: These are set AUTOMATICALLY!
# 🛠️ If using Manual Setup: Set these in your hosting platform's dashboard
# 💻 If running locally: Copy this file to .env and update the values

# Database Configuration (REQUIRED)
# Format: postgresql://username:password@host:port/database
# 
# For local development:
DATABASE_URL=postgresql://localhost:5432/fantasyarena
#
# For Render/Railway: This is automatically set by the platform
# For manual setup: Copy the "External Database URL" from your database dashboard

# Environment (RECOMMENDED)
# Values: "production" for deployed apps, "development" for local
NODE_ENV=development

# Server Port (OPTIONAL - Usually Auto-Set)
# Render/Railway set this automatically - you rarely need to change it
# For local development, defaults to 5000
PORT=5000

# Optional: Replit Integration
# Only needed if running on Replit platform
# REPL_ID=your-repl-id
